# 전파정책 AI — 의존성 버전 고정(lock)
# 목적: pip 무고정으로 라이브러리가 멋대로 자동 최신화되어 갑자기 깨지는 사고 방지.
#       (supabase-py HTTP/2 끊김 사고 — 배경역사 #15)
# 이 조합은 2026-06-20 크롤 #201 정상 실행에서 검증된 버전 세트.
# 모든 GitHub Actions 워크플로가 `pip install -r requirements.txt`로 이 파일을 사용.
# 새 버전으로 올릴 때: 한 번에 하나씩 올리고 워크플로 Run으로 검증 후 커밋(Dependabot 권장).
# PC 로컬도 동일 환경을 원하면 `pip install -r requirements.txt`.

# ── Supabase 계열 (HTTP/2 회피 관련 핵심) ──────────────────
supabase==2.31.0
postgrest==2.31.0
storage3==2.31.0
supabase-auth==2.31.0
supabase-functions==2.31.0
realtime==2.31.0
httpx==0.28.1
httpcore==1.0.9
h11==0.16.0
h2==4.3.0
hpack==4.1.0
hyperframe==6.1.0
anyio==4.14.0
sniffio==1.3.1

# ── Anthropic(요약·긴급도·스타일 증류) ─────────────────────
anthropic==0.111.0
jiter==0.15.0
distro==1.9.0
docstring-parser==0.18.0

# ── 데이터 검증·인증·직렬화 ────────────────────────────────
pydantic==2.13.4
pydantic-core==2.46.4
annotated-types==0.7.0
typing-extensions==4.15.0
typing-inspection==0.4.2
pyjwt==2.13.0
cryptography==49.0.0
cffi==2.0.0
pycparser==3.0
deprecation==2.1.0
packaging==26.2
strenum==0.4.15
websockets==15.0.1
yarl==1.24.2
multidict==6.7.1
propcache==0.5.2

# ── HTTP 요청 ──────────────────────────────────────────────
requests==2.34.2
urllib3==2.7.0
charset-normalizer==3.4.7
idna==3.18
certifi==2026.6.17
pysocks==1.7.1
python-dotenv==1.0.1

# ── 크롤·본문 파싱(크롤러·refetch) ─────────────────────────
beautifulsoup4==4.15.0
soupsieve==2.8.4
feedparser==6.0.12
sgmllib3k==1.0.0
googlenewsdecoder==0.1.7
selectolax==0.4.10
trafilatura==2.1.0
lxml==6.1.1
lxml-html-clean==0.4.5
justext==3.0.2
htmldate==1.10.0
courlan==1.4.0
dateparser==1.4.1
babel==2.18.0
tld==0.13.2
regex==2026.5.9
pytz==2026.2
tzlocal==5.4.3

# ── 공통 유틸 ──────────────────────────────────────────────
python-dateutil==2.9.0.post0
six==1.17.0
